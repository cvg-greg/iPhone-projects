# Project memory

## Artifact delivery

Any artifact generated in a session must also be delivered in portable form:
provide a download of the standalone HTML file (and/or a PDF) alongside the
claude.ai artifact link, so the deliverable is never locked to the Claude
console. The one exception is when security or sensitivity dictates the
content should not be permanent or portable — in that case, keep it
link-only and say why.
