# LEDGER PATCH · 2026-09-01 — mark the road after P22 as RULED (P23 next)

**Target:** `~/Desktop/SPAfiles/SIGNALPOINT-SYSTEMS-LEDGER.md`, §5, the
PLATFORM block ONLY. Two small in-place edits — everything else copies
forward untouched (board protocol: no other block is touched; no history is
added). Apply by hand or let the P23 kickoff session apply it as its first
act; either way the P23 session's own WRAP does the full block rewrite.

---

## Edit 1 — the block's header line

**FIND** (the tail of the `### PLATFORM (the build) — …` header):

```
NEXT = the road after P22 = GW's call — the board holds P23 governance, order staged)
```

**REPLACE WITH:**

```
NEXT = P23 governance — RULED by GW 09-01; order + prep note staged)
```

## Edit 2 — the to-do's lead sentence

**FIND** (the opening of **To-do:**):

```
**NEXT SESSION = the road after P22, GW's call at this wrap** — the board
holds **P23 Governance/Audit Console** (work order STAGED
`Project-Session-Notes/2026-08-31_SIGNALPOINT_phase-23-work-order.md`
```

**REPLACE WITH:**

```
**NEXT SESSION = P23 Governance/Audit Console — RULED (GW, 09-01, remote
prep session)** (work order STAGED
`Project-Session-Notes/2026-08-31_SIGNALPOINT_phase-23-work-order.md` +
prep note STAGED `Project-Session-Notes/2026-09-01_SIGNALPOINT_p23-prep.md`
— read-in proof as far as remote reach allowed, board-refined freeze table
rows 1–7 + the two flags, staged proposals only, zero rulings taken
```

*(The rest of that parenthetical — "the flags: …" onward — stands verbatim;
the new text splices in front of it.)*

---

**Why this patch is legal on the board:** the whiteboard's own protocol says
a block rewrite belongs to the session that worked the system at wrap. This
was a prep session, not a build session — so the patch is deliberately
minimal: it only updates the NEXT pointer (a GW ruling, 09-01) and registers
the staged prep note, so the P23 kickoff opens on a board that reads
current. Nothing else in the block, and no other block, changes.
